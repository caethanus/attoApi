name: atto_api
description: Api do sistema atto.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.0

dependencies:
  postgres: ^3.3.0
  dio: ^5.9.0
  vaden: ^1.0.2
  auto_mapper:
    git:
      url: https://github.com/Luciano-Magnus/auto_mapper.git
      ref: 1.0.11
  drift: ^2.22.0
  drift_postgres: ^1.3.1
  uuid: ^4.5.2

dev_dependencies:
  build_runner: ^2.10.3
  vaden_class_scanner: ^1.0.2
  drift_dev: ^2.22.0

dependency_overrides:
  analyzer: 10.0.0